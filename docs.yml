version: '3'

services:
  docs:
    image: carsons_projects_local_docs
    container_name: carsons_projects_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./carsons_projects:/app/carsons_projects:z
    ports:
      - '9000:9000'
    command: /start-docs
